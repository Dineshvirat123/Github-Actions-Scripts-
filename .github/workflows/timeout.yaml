name: timeout
on: workflow_dispatch
jobs:
    timeout:
        runs-on: ubuntu-latest
        timeout-minutes: 1
        steps:
          - name: display
            run: lsblk
          - name: sleep
            run: sleep 200
